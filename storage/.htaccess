# Deny all access to storage directory via web
# This prevents direct URL access to uploaded files
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Fallback for older Apache versions
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
